# Iteration Roadmap: Second Pass (CRITICAL / IMMEDIATE)

This document tracks the step-by-step execution plan for the current iteration based on the highest priority tasks in `TODO.md`. The focus of this iteration is clearing out the remaining immediate UI and tuning tasks.

## Goal 1: Include Manual Remote Control via the Dashboard
Context: Need to allow manual remote control over the kart via the UI dashboard, reading joystick input through the browser and sending it to ROS2.

**Status: COMPLETE (2026-03-11)**

### Steps:
- [x] 1. **Analyze Dashboard Code:** Read `kb_dashboard` HTML frontend and Python backend to understand the current WebSocket architecture.
- [x] 2. **Add Frontend UI Elements:**
    - Insert a "Manual Control" toggle button.
    - Insert a switch to choose "PWM" vs "Angle" targeting for steering.
    - Add visual bars or indicators for the acceleration/braking axis.
- [x] 3. **Implement Gamepad API:** Write JavaScript to hook into the HTML5 Gamepad API. Poll for joystick input when "Manual Control" is active.
- [x] 4. **Map Inputs to Output Payload:** Map joystick axes correctly (e.g., Left stick X for steering, Right trigger for acceleration). Send JSON payload over WebSocket.
- [x] 5. **Update Backend to Publish ROS2 Messages:** Update `kb_dashboard` python node to listen for manual control messages over the WebSocket and publish them to `/kart/cmd_vel_manual`.
- [x] 6. **Diagnostic Test:** Wrote `test_manual_ws.py` to assert that the `handle_command` triggers `publish_manual_control` with correct values.

---

## Goal 2: Fix Kart Oversteering with YOLO Pipeline
Context: The `steering_gain` parameter is currently too aggressive, causing the kart to oversteer when following cones.

**Status: MOVED TO ITERATION 3 -- see [3.md](3.md)**

---

## Goal 3: Remove Raw Value Display from Dashboard Skins
Context: The dashboard UI still shows raw steering values (like raw potentiometer readings) even though the steering is now calibrated. These should be removed to avoid confusion.

**Status: MOVED TO ITERATION 3 -- see [3.md](3.md)**
