# Iteration 1 Roadmap (CRITICAL / IMMEDIATE)

This document tracks the step-by-step execution plan for the first iteration based on the highest priority tasks in `TODO.md`.

## Goal 1: Investigate and Fix `rviz` Window Accumulation on Orin
Context: A launch file opens multiple rviz instances, causing the Orin to freeze. This is a critical infrastructure bug that blocks UI and testing.

**Status: COMPLETE (2026-03-11)**

### Steps:
- [x] 1. **Codebase Search:** Found all launch files and shell scripts spawning GUI processes. Only `display_zed_cam.launch.py` directly spawns rviz2.
- [x] 2. **Analyze Launch Logic:** Root cause documented in `.agents/investigation_rviz_accumulation.md`. Accumulation caused by relaunches without cleanup.
- [x] 3. **Diagnostic Test:** Created `tests/test_rviz_accumulation.py` -- 4 unit tests verifying cleanup/guard presence across all launch files and shell scripts.
- [x] 4. **Implement Fix:**
    - `run_live.sh` -- Added `killall`/`pkill` cleanup + `trap` for exit (mirroring `run_live_3d.sh`)
    - `display_zed_cam.launch.py` -- Added `ExecuteProcess` cleanup action that kills stale rviz2 before spawning
    - `autonomous.launch.py` -- Added preventive `ExecuteProcess` cleanup for rviz2 and rqt_image_view
    - `gui.launch.py` -- Upgraded to include optional rviz2 node (`use_rviz:=true`) with cleanup, making it the recommended single entry point for visualization
- [x] 5. **Verification:** All 4 diagnostic tests pass.

### Files Modified:
| File | Change |
|---|---|
| `kart_brain/run_live.sh` | Added stale process cleanup + trap |
| `kart_brain/src/ThirdParty/zed_display_rviz2/launch/display_zed_cam.launch.py` | Added ExecuteProcess cleanup before rviz2 spawn |
| `kart_brain/src/kart_bringup/launch/autonomous.launch.py` | Added ExecuteProcess cleanup for GUI processes |
| `kart_brain/src/kart_bringup/launch/gui.launch.py` | Added rviz2 node (conditional), cleanup, launch args |
| `kart_brain/tests/test_rviz_accumulation.py` | New diagnostic test (4 tests) |
