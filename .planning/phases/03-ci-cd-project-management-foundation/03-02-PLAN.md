---
phase: 03-ci-cd-project-management-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ".github/workflows/ci.yml"
  - ".clang-tidy"
  - "pyproject.toml"
autonomous: true
requirements: [METR-01, METR-02]
must_haves:
  truths:
    - "Commits to Pull Requests automatically trigger the CI pipeline"
    - "CI pipeline fails if clang-tidy or Ruff detect code quality violations"
    - "CI pipeline fully compiles ROS 2 nodes and ESP32 firmware"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI automation on PRs"
      contains: "pull_request:"
    - path: ".clang-tidy"
      provides: "Strict C++ linting"
      contains: "WarningsAsErrors: '*'"
    - path: "pyproject.toml"
      provides: "Strict Python linting"
      contains: "tool.ruff.lint"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "ruff check"
      pattern: "ruff check"
    - from: ".github/workflows/ci.yml"
      to: ".clang-tidy"
      via: "clang-tidy execution"
      pattern: "clang-tidy"
---

<objective>
Establish a rigorous CI pipeline that enforces static analysis rules using Ruff and clang-tidy, and compiles code fully in isolated official environments to guarantee code quality on PR merges.

Purpose: Catches code quality issues and build failures before they merge to `main`.
Output: `.github/workflows/ci.yml`, `.clang-tidy`, and `pyproject.toml`.
</objective>

<execution_context>
@/Users/albertorblan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/albertorblan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ci-cd-project-management-foundation/03-CONTEXT.md
@.planning/phases/03-ci-cd-project-management-foundation/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish Strict Linter Configurations</name>
  <files>
    - .clang-tidy
    - pyproject.toml
  </files>
  <action>
    Create `.clang-tidy` configuring C++ lint checks (bugprone, cppcoreguidelines, modernize, performance, readability) and enforcing strict failure (`WarningsAsErrors: '*'`).
    Create `pyproject.toml` configuring Ruff with target Python 3.10, line-length 100, and standard select rules (E, W, F, I, C, B). This enforces modern Python/C++ code hygiene.
  </action>
  <verify>
    <automated>ls pyproject.toml .clang-tidy</automated>
  </verify>
  <done>Global linter config files exist with strict failure enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Create Multi-Container CI Workflow</name>
  <files>
    - .github/workflows/ci.yml
  </files>
  <action>
    Create a GitHub Actions workflow `.github/workflows/ci.yml` that strictly triggers `on: pull_request: branches: [main]`.
    Define three jobs:
    1. **linting**: A lightweight ubuntu job running `pip install ruff` and `ruff check .`
    2. **ros2-build**: A job running in container `ros:humble-ros-base` executing `colcon build --cmake-args -DCMAKE_EXPORT_COMPILE_COMMANDS=ON` and running `clang-tidy` via the generated compilation commands.
    3. **esp32-build**: A job running in container `espressif/idf:v5.3` executing `idf.py build` (assuming standard IDF project structure, or just the framework). Note: if no firm directory exists yet, create an empty one or a placeholder CMake script to avoid failure, or omit strict building until code is present (but prefer failing fast to enforce it).
  </action>
  <verify>
    <automated>gh workflow view ci.yml || echo "Workflow exists"</automated>
  </verify>
  <done>The workflow file is successfully configured to run on PRs and enforces linting/building.</done>
</task>

</tasks>

<verification>
Check `gh workflow view ci.yml` to verify it registers in the CLI correctly, and the configurations are properly strict.
</verification>

<success_criteria>
1. `.clang-tidy` and `pyproject.toml` enforce strict analysis.
2. The GH Actions pipeline triggers on PRs and compiles ROS 2/ESP-IDF.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ci-cd-project-management-foundation/03-02-SUMMARY.md`
</output>