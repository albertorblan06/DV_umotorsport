---
phase: 02-full-autonomous-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["scripts/generate_llms_txt.py", "docs/hardware/sensors_and_actuators.md", "docs/assembly/mounting_guide.md"]
autonomous: true
requirements: ["AUTO-01"]
must_haves:
  truths:
    - "Documentation directory structure exists without any site generators"
    - "Python script can dynamically aggregate markdown files"
    - "Hardware and assembly documentation are written with a strict no-emojis policy"
  artifacts:
    - path: "scripts/generate_llms_txt.py"
      provides: "Aggregation script for LLMs"
      contains: "pathlib"
    - path: "docs/hardware/sensors_and_actuators.md"
      provides: "Actuator and sensor details"
  key_links:
    - from: "scripts/generate_llms_txt.py"
      to: "docs/"
      via: "pathlib.Path.rglob('*.md')"
---

<objective>
Establish the documentation foundation, hardware, and assembly guides for autonomous navigation.

Purpose: Create the AI-optimized documentation generation script and write from scratch the hardware and mechanical assembly documentation needed for Full Autonomous Navigation, strictly adhering to the "no emojis" and professional tone constraints.
Output: The `scripts/generate_llms_txt.py` script and initial Markdown files in `docs/hardware` and `docs/assembly`.
</objective>

<execution_context>
@/Users/albertorblan/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/albertorblan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-autonomous-navigation/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Architecture and Aggregation Script</name>
  <files>scripts/generate_llms_txt.py</files>
  <action>Create the documentation directory structure (`docs/hardware`, `docs/software`, `docs/assembly`, `docs/integration`). Then, write `scripts/generate_llms_txt.py` from scratch. The script must dynamically traverse the `docs/` folder (using `os.walk` or `pathlib.Path.rglob('*.md')`) to read all `.md` files and generate two files in the project root: `llms.txt` and `llms-full.txt`. Do NOT use mkdocs or any site generator.</action>
  <verify>
    <automated>python3 scripts/generate_llms_txt.py && ls llms.txt llms-full.txt</automated>
  </verify>
  <done>Directories are created and the script runs without errors, generating empty or minimal LLM text files.</done>
</task>

<task type="auto">
  <name>Task 2: Write Hardware and Assembly Docs</name>
  <files>docs/hardware/sensors_and_actuators.md, docs/assembly/mounting_guide.md</files>
  <action>Write the hardware documentation (detailing ZED camera, ESP32 wiring, and actuators) and assembly documentation (mechanical constraints, mounting instructions) for full autonomous navigation from scratch. **CRITICAL:** Do NOT copy/paste from the `heritage/` folder. Ensure a strict professional tone with ZERO emojis.</action>
  <verify>
    <automated>grep -vq "😊\|🚀" docs/hardware/sensors_and_actuators.md && wc -l < docs/hardware/sensors_and_actuators.md | awk '{if ($1 > 10) print "pass"; else exit 1}'</automated>
  </verify>
  <done>Hardware and assembly markdown files are populated with professional, emoji-free technical content.</done>
</task>

</tasks>

<verification>
Ensure the directories exist, the Python script correctly concatenates the markdown files, and the files strictly contain zero emojis.
</verification>

<success_criteria>
Script successfully generates `llms.txt` and docs contain substantial professional content.
</success_criteria>

<output>
After completion, create `.planning/phases/02-full-autonomous-navigation/02-01-SUMMARY.md`
</output>
